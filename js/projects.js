/* ============================================================
   PROJECTS PAGE — project filtering and video modal live in global.js
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  /* Intentionally minimal: [data-category] + .filter-btn are wired in js/global.js */
});
